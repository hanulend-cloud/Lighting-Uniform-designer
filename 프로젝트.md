Uniform Desing Simulator - Project master document

##1. 프로젝트 개요 (Project Overview)
* ** 목적 : 조명기구 설계시 1. 염가화(최소부품), 2 생산성향상 (조립/제작 용이성) 을 달성하면서도 3. 균일한 품질 달성을 위한 최적안 도출
*. ** 개발전략 : 복잡한 3d시각화 프로그램을 바로 실행하지 않고, 수학적/광학적 계산 기반의 **1차 사양 설계 엔진** 과 시각적 상세 설계 검증을 위한 **2차 Visual 시뮬레이터** 로 이원화 하여 연산 자원과 설계 시간을 최적화 함

##2. 시스템 아키텍처 및 기술 스택 
UI/UX방향 : 웹기반 광학연산 및 Visual 렌더링(몬테-카를로) 대시보드 GUI 

### 기술 스택 (구체화 필요)
* **연산 엔진 : Python(NumPy/SciPy) 또는 순수 JS. 광학 근사식·최적화 담당
* **프론트 : 웹 대시보드(React 등) + 2D 히트맵/프로파일 차트, Phase2는 WebGL 렌더링
* **데이터 : 입력/출력을 JSON 스키마로 정의(저장·불러오기·재현성)

### 적용 범위 (Scope)
* **Phase 1 : 직하형(Direct-lit) 방식 한정
* **엣지형 도광판(LGP) : 컨셉이 달라 전용 엔진 필요 → Phase 2에서 별도 모듈로 추가

### Stage1. : 1차 사양 설계 엔진
수식과 휴리스틱(경험치)를 통해 근사치를 찾고 최적의 Trade-off옵션을 산출하여 상세설계로 진행하기 위한 단계
* **비용 평가 알고리즘" 광원 수량 비용 + 기구물 개수 + 기구물 형상의 난이도 + 패턴 어레이  등으로 제작 난이도 패널티
* **기구물 개수는 한개의 바디로 균일도를 확보하지 못하고 개수를 추가해야할때
* **기구물 난이도1 평면형상에서 두께를 증가하거나 감소함. 
* **기구물 난이도2 전체두께를 두껍게 하거나, LED가 위치한 부분의 두께를 두껍게 변형함.
* **기구물 난이도3 LED위치에 형상을 변경하여 LED하여 gap을 최소화(1~2 mm) 하고 LED와 먼곳은 GAP을 키움이
* **기구물 난이도4 형상을 변경하고 소재에 확산소재를 적용하여 빛을 산란시킴
* **패턴어레이(기구물 난이도5) 볼록이나 오목형태의 패턴(프리즘, 실린더)이나 미세형상(dot, pyramid) 어레이로 빛을 산란시킴 
* **자동 차원 분류기 (2d/3d) 광원 지향각과 조명 깊이를 분석 Y축 폭과 Y축 LED지향각도에 따라 2D(선형)/3D(평면) 배열 자동 판단.
* **균일도 계산은 LED의 지향각(FWHM x, FWHM y)를 고려하고 거리의 역제곱 법칙을 기본으로 최종 광량분포를 계산.
* **균일도 계산은 Lambertian/Gaussian으로 선택 
* **광경로 및 광량분포는 일반적인 광학법칙 : 반사, 투과, 굴절(snell's law), 전반사, 확산, 프레넬 반사 등의 물리법칙을 적용
* **직하형 균일도 모델 : 광학거리 OD와 LED pitch 비율(OD/pitch), 하우징 반사판 반사율, 확산판 헤이즈·게인·매수를 입력으로 출광면 균일도를 1차 예측(직하형 백라이트 표준 설계식 기반)
* **근접장 처리 : 관측면이 LED 발광면의 5배 거리 이내이면 점광원 근사 대신 발광면(2x2mm)을 격자 분할하여 적분
* **광원 배광 입력 : Lambertian/Gaussian 근사 외 실측 배광(IES/LDT) 파일 임포트, LED 광속(lm)·비닝·열저하(thermal derating) 반영
* **평가면 : 기구 출광면 바로 위(Z≈0, 약 1mm offset). 유효영역 = 타겟 전체(기본, 판정제외 마진 0). 베젤이 가장자리를 가리는 제품만 goal.edgeMargin(축별 비율)으로 그 폭을 제외
* **균일도 지표 : 1순위 U0 = E_min/E_avg(일반조명 표준), 보조 CV(RMSE)=σ/E_avg·국부 gradient(인접점 변화율 %/pitch). 참고 E_min/E_max diversity, 면발광형은 13점 휘도 min/max. 중앙 수평·수직 라인 프로파일 병행. 조도(lux)/휘도(cd/m²) 기준 선택
* **색균일도 : RGB/멀티 CCT 광원 선택 시 Δu'v'·CCT 편차를 별도 KPI로 계산
* **로버스트니스 : LED 실장 위치·광축·광속 산포(공차) 입력 시 균일도 최악값·표준편차 산출
* **2D/3D 분류기 임계값 : Y축 폭·지향각 기준 판정 경계값을 수치로 정의

### Stage2. : 2차 Visual 시뮬레이터 (Ray Tracing & Rendering) Phase2 개발
 1차 엔진의 결과(LED Pitch,수량, ,기구구조)를 바탕으로 3D공간 구성 및 시각적 검증
 1차 엔진 설계에서 선정된 방법론에 해당하는 기구물 난이도별 적용되는 설계인자의 사양을 변경 반영할수 있어야 함.
* **기구구조 3d형상 설계 가이드 및 자동설계 균일도 최적화
* **기구구조 LED 위치는 거리를 가깝게 하고 led가 없는 위치는 거리를 띄워 광확산을 이용이용 균일도 확보
* **미세패턴 형상 어레이 설계 가이드 및 자동설계 균일도 최적화
* **마이크로 크기의 동일한 어레이패턴ㅁㄴㅇㄹ으로 (삼각형,반구,피라미드)형상을 이용한 광산란으로 균일도를 확보
* **기구소재 변경 (굴절율, 확산도)최적화를 통한 균일도 확보 설계
* **몬테카를로 광선추척(Monte Carlo Ray Tracing) 기반 조도 맵핑 및 히트맵 렌더링


##3. 핵심 설계 인자 및 제약 조건 (Parameters & Constrains)
### [입력 사양 (Input)]
1. ** 타겟영역** 가로길이(x mㅇm), 세로폭(y mm), 형상(평면, 곡선, 지정모양)
2. ** 목표값** 목표 균일도 U0 = E_min/E_avg (예 0.7, 일반조명 표준). 보조: CV(RMSE) ≤ 0.15, 국부 gradient ≤ 20%/pitch. 참고: E_min/E_max
3. ** 광원(LED)** Size(기본 2x2x0.5mm, 지향각(X,Y Beam Angle, 기본 120도), 파장(R,G,B,White선택가능)
4. ** 공간** 기구 내부 설계 가능깊이 (Depth, mm)
5. ** 기구사양1** 기구 두께 및 소재 (기본 3mm, 투명 Poly-carbonate)
6. ** 기구사양2** LED위치 범위 두께 증가 가능 (기본 LED와 동일크기 3mm도출)
7. ** 기구사양3** 기구부 하면 ray (기본 0.5mm Radius, 깊이 0.25mm, 간격 X,Y pitch 3mm)
8. ** 하우징 반사판** 내벽 반사율(기본 90%, 확산반사), 반사판 유무 선택
9. ** 기구물(최종 광학부품)** 별도 확산판 없음. 기준 두께·굴절률 n·측벽 반사율. 산란은 난이도별 소재 haze(L4)·미세패턴(L5)·형상에서만 발생. LED→상면 거리 = '사용 깊이'. 관찰면 = 상면 +0.1mm
10. ** 광원 광학량** LED 광속(lm) 또는 광도(cd), 실측 배광(IES) 파일(선택)
11. ** 전력/열** 소비전력(또는 If·Vf), 허용 정션온도, 방열 조건
12. ** 생산 조건** 로트/연간 생산수량(금형비 상각 기준), 목표 단가(선택)

### [출력 사양(Output)]
1. 추천 설계 옵션 (Top 2) : 비용, 난이도 고려 2가지 방안 조합 제공
2. ** 상세 데이터** 추천 조합 형태구조, 필요 LED수량, LED간격(Pitch, mm), 예상균일도(%)
3. 최종출력 그래프 2D Top view, 2D Side View, isometric(등각) view
4. ** 출력 그래프에서 X,Y SIZE비율은 유지
5. 타겟영역 광분포 시각 이미지화 표시
6. 실시간 INPUT입력값 반영하여 결과 반영
7. ** 균일도 리포트** 다중 지표·색균일도·라인 프로파일 수치표
8. ** 전력/효율** 총 소비전력, 시스템 광효율(lm/W), 광학 손실 내역
9. ** 설계 사양서 내보내기** BOM(부품·수량·단가) 포함 PDF/CSV, 입력값 JSON 저장·불러오기
10. 설계안 A/B 비교 뷰

### [예외 처리(Edge Case)]
* 입력된 깊이(Depth)가 0이하, 목표균일도가 1.0(100%) 초과하는경우 등 물리적으로 불가능한 값 입력시 에러 및 재입력 유도
* 주어진 Depth공간 내에서 목표, 균일도 달성이 불간으한경우, 최소필요 Depth 연산하여 제안필요.

##3.5 최적화 엔진 (Optimization Engine)
* **목적함수 : 비용·제작난이도·균일도(및 색균일도)의 다목적 최적화. 가중치 프로파일(염가우선/품질우선/균형) 선택
* **탐색 방법 : 1차 격자 탐색으로 후보 압축 → 국소 최적화(Nelder-Mead/베이지안). 필요 시 NSGA-II로 Pareto front 산출
* **설계 변수 : LED pitch·수량, OD, 기구 두께 프로파일, 확산소재/패턴 난이도 레벨(1~5), 반사판 반사율
* **설계 기본 원칙 : 기구 변경 없이 LED 배치·개수만으로 균일도를 먼저 확보(L1). 판정 영역 = 타겟 면적의 중심 95%(goal.centerArea, 각 변 1.27% 안쪽), 타겟 전체 min/max 와 테두리 초과 밝기는 참고값. LED 는 개수 n 을 정해 타겟 중심 대칭으로 기구 경계(타겟+오버행, 각 변 최대 +5%, LED 반폭 안쪽)까지 균등하게 펼친다 — 추가는 중심부 기준으로 촘촘해지고, 제거는 가장자리 지원부터 줄어든다("중심부 목표를 만족하는 최소 n"). 중심에 LED 가 꼭 있을 필요는 없다 — 성긴 쪽 위상은 피치 ≥ 허용폭이면 양끝 2열(중심 비움), 피치 ≥ 2·허용폭이면 1열 중앙(예: 100×11 깊이 7 은 양끝 2열 + X 피치 12mm, 20개가 최소). 열이 3개 이하인 축은 최외곽 열을 타겟 안쪽으로 들이는(음수 오버행, 기구 크기는 그대로) 배치도 탐색한다 — 열이 적으면 최외곽 열 위치가 프로파일을 정하므로 양끝 배치와 중앙 몰림 사이에 최적이 있다(실측 100×6 깊이 7: 양끝 81% → y=1/5 에서 90%; 100×10: 3열 39개 → 2열 안쪽 26개). 균일도는 피치≈깊이 근처에서 최고이고 더 촘촘해도 가장자리 한계로 내려가므로 솔버는 비단조 탐색(성긴 쪽부터 훑기+국소 이진탐색). 목표 미달 시 대표안 = 중심부 균일도 최고 배치. LED 수 하한(6개)은 연성 조건 — 강확산에서 6개 이상으로 목표를 못 채우면 하한 미만 배치를 "달성(LED 하한 미만)"으로 보고. 열 수가 바뀌는 경계에서 feasible 피치가 여러 섬으로 갈라지므로 탐색은 항상 성긴 쪽부터 아래로 훑는다
* **Top 2 선정 : Pareto front에서 '최저비용 실현안'과 '균일도 여유 확보안' 자동 선정, 트레이드오프 근거 표기
* **민감도 분석 : 각 변수의 균일도·비용 기여도 순위 출력
* **자원 제약 : 최대 반복수·목표 허용오차·연산시간 예산 파라미터화

##3.6 비용·DFM 모델 (Cost & DFM Model)
* **정량 단가 : LED 개당 단가 + 재료비(g단가×체적) + 공정비(조립 표준시간×임률) + 확산 첨가제·패턴 가공비 + 금형비 상각(=금형가/생산수량)
* **사출 성형 제약 : 최소 살두께, 이형구배(draft), 언더컷, 게이트 위치, 웰드라인 위험 → 난이도 3~5 형상안에 성형 가능성 플래그
* **손익분기 : 생산수량별 옵션 총원가 곡선과 교차점 제시

##3.7 검증·단위계 (Validation & Units)
* **단위계 : 길이 mm, 각도 deg, 조도 lux, 광도 cd, 광속 lm, 휘도 cd/m², 전력 W. 좌표계는 출광면 중심 원점, +Z 출광방향
* **Stage1↔Stage2 정합 : 동일 입력에 대한 1차 근사식과 몬테카를로 결과의 균일도 오차 허용범위(예 ±3%p) 정의, 초과 시 캘리브레이션
* **벤치마크 : 해석해 케이스(단일 LED 조도분포, 무한 어레이 균일도) + 실측 1건 이상으로 회귀 검증 세트 유지

##3.8 개발 단계 및 완료 기준 (Phasing)
* **Phase 1 (Stage1) : 직하형 한정, 수식·휴리스틱 기반 1차 사양 도출. 완료 기준 — 벤치마크 케이스에서 균일도 예측 오차 ±5%p 이내, Top2 옵션·BOM·리포트 자동 생성
* **Phase 2 (Stage2) : 몬테카를로 광선추적 3D 검증, 기구 3D 형상·미세패턴 자동 최적화, 소재/굴절률 정밀 튜닝
* **Phase 2 추가 모듈 : 엣지형 도광판(LGP) — 도트/V-cut 밀도 그라디언트 설계 전용 엔진

##4. 업데이트 및 개발을 위한 프롬프트 가이드 (Prompt Iteration Log)

> **[사용방법]** AI에게 코드를 요구할때, 이 문제 전체를 컨텍스트로 제공한위 아래의 프롬프트 입력

### Base Prompt (컨텍스트 주입 및 초기화)
너는 조명설계 광학전문가 이고 조명설계 및 최적화 시뮬레이터를 개발하는 수석 엔지니어 AI야
이 Markdown 문서에 정의된 목적, 아키텍처, 파라미터를 기반으로작업을 수행해
모든 코드는 유지보수가 쉽도록 객체지향적으로 작성하고, 모든 부품의 예상비용과 난이도를 고려해서 DFM에 반영해줘.







